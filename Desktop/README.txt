To install and use random.py, download random.py. To run it, you need some sort of program that can run Python, like MacPython. Then, in IDLE, you can run random(n).

The function generates a random inte